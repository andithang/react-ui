# Empty

`Empty` displays no-data states with optional description and actions.

## Props

- `image` and `imageStyle`
- `description`
- `children` for actions

## Static images

- `Empty.PRESENTED_IMAGE_DEFAULT`
- `Empty.PRESENTED_IMAGE_SIMPLE`
